<Window x:Class="VideoFetch.Views.VideoPlayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VideoFetch Player" Width="960" Height="600"
        MinWidth="640" MinHeight="400"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BgPrimaryBrush}"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <!-- Player time slider: thick track + purple accent + white thumb -->
        <Style x:Key="PlayerSlider" TargetType="Slider">
            <Setter Property="Height" Value="30"/>
            <Setter Property="IsMoveToPointEnabled" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Border x:Name="TrackArea" Height="30" Background="Transparent">
                            <Grid VerticalAlignment="Center">
                                <!-- Track background -->
                                <Border Height="6" CornerRadius="3" Background="#3A3A5A" Margin="9,0"/>
                                <!-- Track -->
                                <Track x:Name="PART_Track" IsDirectionReversed="False">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="Slider.DecreaseLarge">
                                            <RepeatButton.Template>
                                                <ControlTemplate TargetType="RepeatButton">
                                                    <Border Height="6" CornerRadius="3,0,0,3"
                                                            Background="#7C6FF7"/>
                                                </ControlTemplate>
                                            </RepeatButton.Template>
                                        </RepeatButton>
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="Slider.IncreaseLarge">
                                            <RepeatButton.Template>
                                                <ControlTemplate TargetType="RepeatButton">
                                                    <Border Height="6" Background="Transparent"/>
                                                </ControlTemplate>
                                            </RepeatButton.Template>
                                        </RepeatButton>
                                    </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb Width="16" Height="16" Cursor="Hand"
                                               DragStarted="TimeSlider_DragStarted"
                                               DragCompleted="TimeSlider_DragCompleted">
                                            <Thumb.Template>
                                                <ControlTemplate TargetType="Thumb">
                                                    <Ellipse Width="16" Height="16"
                                                             Fill="White"/>
                                                </ControlTemplate>
                                            </Thumb.Template>
                                        </Thumb>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Volume slider -->
        <Style x:Key="VolumeSliderStyle" TargetType="Slider">
            <Setter Property="Height" Value="20"/>
            <Setter Property="IsMoveToPointEnabled" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Border Height="20" Background="Transparent">
                            <Grid VerticalAlignment="Center">
                                <Border Height="4" CornerRadius="2" Background="#3A3A5A"/>
                                <Track x:Name="PART_Track">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="Slider.DecreaseLarge">
                                            <RepeatButton.Template>
                                                <ControlTemplate TargetType="RepeatButton">
                                                    <Border Height="4" CornerRadius="2"
                                                            Background="White"/>
                                                </ControlTemplate>
                                            </RepeatButton.Template>
                                        </RepeatButton>
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="Slider.IncreaseLarge">
                                            <RepeatButton.Template>
                                                <ControlTemplate TargetType="RepeatButton">
                                                    <Border Height="4" Background="Transparent"/>
                                                </ControlTemplate>
                                            </RepeatButton.Template>
                                        </RepeatButton>
                                    </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb Width="10" Height="10" Cursor="Hand">
                                            <Thumb.Template>
                                                <ControlTemplate TargetType="Thumb">
                                                    <Ellipse Width="10" Height="10" Fill="White"/>
                                                </ControlTemplate>
                                            </Thumb.Template>
                                        </Thumb>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <MediaElement x:Name="Media" Grid.Row="0"
                      LoadedBehavior="Manual" UnloadedBehavior="Stop"
                      Volume="1.0"
                      MediaOpened="Media_MediaOpened"
                      MediaEnded="Media_MediaEnded"/>

        <Border Grid.Row="1" Background="#1A1A2E"
                BorderBrush="#252540" BorderThickness="0,1,0,0"
                Padding="14,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Play/Pause -->
                <Button x:Name="BtnPlayPause" Grid.Column="0"
                        Width="36" Height="36" Click="BtnPlayPause_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Border" Background="Transparent" CornerRadius="4"
                                    Width="36" Height="36">
                                <Path x:Name="Icon" Stretch="Uniform" Fill="#CCC"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Width="12" Height="12"
                                      Data="M 1,1 L 11,6 L 1,11 Z"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#2A2A44"/>
                                    <Setter TargetName="Icon" Property="Fill" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Volume button -->
                <Button x:Name="BtnVolumeMute" Grid.Column="1"
                        Width="32" Height="32" Margin="4,0,0,0"
                        Click="BtnVolumeMute_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Border" Background="Transparent" CornerRadius="3"
                                    Width="32" Height="32">
                                <Path x:Name="Icon" Stretch="Uniform" Fill="#999"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Width="14" Height="12"
                                      Data="M 1,4 L 4,4 L 8,1 L 8,11 L 4,8 L 1,8 Z"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#2A2A44"/>
                                    <Setter TargetName="Icon" Property="Fill" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Volume slider -->
                <Slider x:Name="VolumeSlider" Grid.Column="2"
                        Style="{StaticResource VolumeSliderStyle}"
                        Minimum="0" Maximum="100" Value="100"
                        Width="80" VerticalAlignment="Center"
                        ValueChanged="VolumeSlider_ValueChanged"/>

                <!-- Time slider -->
                <Slider x:Name="TimeSlider" Grid.Column="3"
                        Style="{StaticResource PlayerSlider}"
                        Minimum="0" Maximum="100" Value="0"
                        VerticalAlignment="Center"/>

                <!-- Time -->
                <TextBlock x:Name="TimeLabel" Grid.Column="4"
                           Text="00:00 / 00:00" FontSize="11" FontFamily="Consolas"
                           Foreground="#AAAAAA" VerticalAlignment="Center" Margin="10,0,10,0"/>

                <!-- Close -->
                <Button Grid.Column="5" Content="✕" Width="32" Height="32"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Click="BtnClose_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>
